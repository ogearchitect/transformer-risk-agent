[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transformer-risk-agent"
version = "0.1.0"
description = "Agentic AI demo for transformer failure risk assessment"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
